<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for future features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_tripbuddy_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_tripbuddy_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Itmba2formative">

        <!-- Welcome/Splash Screen Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TripBuddy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activity -->
        <activity
            android:name=".AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.TripBuddy.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.TripBuddy.NoActionBar"
            android:launchMode="singleTop" />

        <!-- Memory Activity -->
        <activity
            android:name=".MemoryActivity"
            android:exported="false"
            android:theme="@style/Theme.TripBuddy.NoActionBar"
            android:parentActivityName=".HomeActivity">
        </activity>

        <!-- Gallery View Activity -->
        <activity
            android:name=".GalleryActivity"
            android:exported="false"
            android:theme="@style/Theme.TripBuddy.NoActionBar"
            android:parentActivityName=".HomeActivity">
        </activity>

        <!-- Budget/Trip Planning Activity -->
        <activity
            android:name=".BudgetActivity"
            android:exported="false"
            android:theme="@style/Theme.TripBuddy.NoActionBar"
            android:parentActivityName=".HomeActivity">
        </activity>

        <!-- Database Debug Activity -->
        <activity
            android:name=".DatabaseActivity"
            android:exported="false"
            android:theme="@style/Theme.TripBuddy.NoActionBar"
            android:parentActivityName=".HomeActivity">
        </activity>

        <!-- Profile/Settings Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.TripBuddy.NoActionBar"
            android:parentActivityName=".HomeActivity">
        </activity>

        <!-- File Provider for Camera and Image handling -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
        </provider>

    </application>

</manifest>